#!/usr/bin/env bash
# Mock `cursor` for tests/test_install.sh. install.sh only ever probes for
# this binary's presence via `command -v cursor`; it never executes it, so
# this stub exists purely to be found on PATH.
exit 0
